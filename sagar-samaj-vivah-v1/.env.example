# Database Connection
# Since you are running MongoDB locally on the VM:
DATABASE_URL="mongodb://localhost:27017/sagar_vivah"

# Authentication Secret (Run `openssl rand -base64 32` to generate one)
NEXTAUTH_SECRET="change-this-to-a-secure-random-string"
NEXTAUTH_URL="http://localhost:3000"
